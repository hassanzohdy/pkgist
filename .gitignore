node_modules
# Anchored to the package root: a bare `dist` / `build` would also match
# `src/build/` and silently untrack the build-pipeline source. Keep the leading
# slash so only the root output dirs are ignored.
/dist
/build

# Local release config — each consumer writes their own.
# See pkgist.config.example.ts for the template, or run `pkgist init`.
pkgist.config.ts